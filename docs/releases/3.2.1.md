# Calliope AI CLI 3.2.1

Status: **published**. This patch refreshes the npm-facing README with the major
3.2 workbench capabilities and links the complete 3.2 release notes.

The implementation is unchanged from 3.2.0. The package metadata, changelog and
README are published together so the npm package page exposes the same feature
summary as the tagged release.
