# Calliope AI CLI 3.2.0 release preparation

Status: **unpublished; release gates remain blocked**. The package version is
prepared for 3.2.0; npm still serves 3.1.0. No release tag or publication is implied.

The foundation shares turn execution and permissions across terminal, headless
and ACP, propagates cancellation, loads trusted scoped instructions, preserves
Bedrock reasoning state and uses Google's `@google/genai` SDK. DeepSeek, xAI and
Cerebras use provider profiles; AI21 is retired. The conformance suite exercises
the installed SDKs through intercepted HTTP and separately replays real captures.

This release preparation corrects the active conformance matrix, adds an offline
machine-readable readiness inventory, and bounds live probes with persistent
dollar reservations. Strict model discovery exposes failed requests instead of
reporting emergency fallbacks as current provider evidence.

## Evidence and blockers

- [Provider conformance](../provider-conformance.md): current matrix and commands.
- [Original live report](../provider-live-testing.md): historical capture provenance.
- [Capture follow-up](../provider-live-followup.md): 12 new captures, provider
  failures, retained reservations and the user-requested xAI deferral.
- [Latest release audit](../reports/provider-capture-followup-2026-09-11.json): access,
  missing checks and bounded inference attempts. Historical captures are retained;
  model listing alone does not prove inference works.
- #260 merged into the stacked runtime branch, not main. Foundation PRs #256,
  #258 and #259 still require review and integration before a main-based release.
- All active-provider wire combinations and extended semantic evidence must be
  completed. xAI live testing is explicitly deferred at the user's request; its
  missing evidence does not count as passing, and the full gate remains closed.
- The full product roadmap's doctor, orchestration and brain/KG smoke gates remain
  pending implementation. The seven phases are recorded on
  [#254](https://github.com/calliopeai/calliope-cli/issues/254#issuecomment-5629931337).
- Release signing/provenance remains tracked in #223.

## Validation commands

```sh
npx tsc --noEmit
npm test -- --maxWorkers=2
npm run test:coverage -- --maxWorkers=2
npm run test:conformance
npm run test:conformance:release
npm run build
npm run bench
node scripts/conformance/report.mjs --json --require-ready
```

Publication is conditional on current passing evidence, clean install/headless
smokes, a secret-free clean git tree, green CI and the applicable approval gates.
Never bypass `prepublishOnly` to work around missing captures.
